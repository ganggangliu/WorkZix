/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#include <lcm/lcm_coretypes.h>

#ifndef __LCM_TRAFIC_LIGHT_RESULT_hpp__
#define __LCM_TRAFIC_LIGHT_RESULT_hpp__

#include "LCM_TRAFIC_LIGHT_REQUIRE.hpp"


class LCM_TRAFIC_LIGHT_RESULT
{
    public:
        LCM_TRAFIC_LIGHT_REQUIRE Req;
        int8_t     State;

    public:
        inline int encode(void *buf, int offset, int maxlen) const;
        inline int getEncodedSize() const;
        inline int decode(const void *buf, int offset, int maxlen);
        inline static int64_t getHash();
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static int64_t _computeHash(const __lcm_hash_ptr *p);
};

int LCM_TRAFIC_LIGHT_RESULT::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int LCM_TRAFIC_LIGHT_RESULT::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int LCM_TRAFIC_LIGHT_RESULT::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t LCM_TRAFIC_LIGHT_RESULT::getHash()
{
    static int64_t hash = _computeHash(NULL);
    return hash;
}

const char* LCM_TRAFIC_LIGHT_RESULT::getTypeName()
{
    return "LCM_TRAFIC_LIGHT_RESULT";
}

int LCM_TRAFIC_LIGHT_RESULT::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = this->Req._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->State, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int LCM_TRAFIC_LIGHT_RESULT::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = this->Req._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->State, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int LCM_TRAFIC_LIGHT_RESULT::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += this->Req._getEncodedSizeNoHash();
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    return enc_size;
}

int64_t LCM_TRAFIC_LIGHT_RESULT::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == LCM_TRAFIC_LIGHT_RESULT::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, (void*)LCM_TRAFIC_LIGHT_RESULT::getHash };

    int64_t hash = 0xe8b24a75012fa65fLL +
         LCM_TRAFIC_LIGHT_REQUIRE::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

#endif
