/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#include <lcm/lcm_coretypes.h>

#ifndef __Av2HR_ADM_LANE_LINE_PROFILE_hpp__
#define __Av2HR_ADM_LANE_LINE_PROFILE_hpp__

#include <vector>
#include "Av2HR_ADM_OUTLINE.hpp"


class Av2HR_ADM_LANE_LINE_PROFILE
{
    public:
        int8_t     m_iLaneLineNum;
        std::vector< Av2HR_ADM_OUTLINE > m_outlineString;

    public:
        inline int encode(void *buf, int offset, int maxlen) const;
        inline int getEncodedSize() const;
        inline int decode(const void *buf, int offset, int maxlen);
        inline static int64_t getHash();
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static int64_t _computeHash(const __lcm_hash_ptr *p);
};

int Av2HR_ADM_LANE_LINE_PROFILE::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int Av2HR_ADM_LANE_LINE_PROFILE::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int Av2HR_ADM_LANE_LINE_PROFILE::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t Av2HR_ADM_LANE_LINE_PROFILE::getHash()
{
    static int64_t hash = _computeHash(NULL);
    return hash;
}

const char* Av2HR_ADM_LANE_LINE_PROFILE::getTypeName()
{
    return "Av2HR_ADM_LANE_LINE_PROFILE";
}

int Av2HR_ADM_LANE_LINE_PROFILE::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->m_iLaneLineNum, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->m_iLaneLineNum; a0++) {
        tlen = this->m_outlineString[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int Av2HR_ADM_LANE_LINE_PROFILE::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->m_iLaneLineNum, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    this->m_outlineString.resize(this->m_iLaneLineNum);
    for (int a0 = 0; a0 < this->m_iLaneLineNum; a0++) {
        tlen = this->m_outlineString[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int Av2HR_ADM_LANE_LINE_PROFILE::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->m_iLaneLineNum; a0++) {
        enc_size += this->m_outlineString[a0]._getEncodedSizeNoHash();
    }
    return enc_size;
}

int64_t Av2HR_ADM_LANE_LINE_PROFILE::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == Av2HR_ADM_LANE_LINE_PROFILE::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, (void*)Av2HR_ADM_LANE_LINE_PROFILE::getHash };

    int64_t hash = 0x73e7ce7ae04af2c1LL +
         Av2HR_ADM_OUTLINE::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

#endif
