/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#include <lcm/lcm_coretypes.h>

#ifndef __LCM_SENSOR_FUSION_PACKAGE_hpp__
#define __LCM_SENSOR_FUSION_PACKAGE_hpp__

#include <vector>
#include "LCM_GPS_DATA.hpp"
#include "LCM_IBEO_CLOUD_POINTS.hpp"
#include "LCM_IBEO_OBJECT_LIST.hpp"
#include "LIDAR_RADAR_OBJECT_INFO.hpp"
#include "LCM_NAVI_TO_SENSE_INFO.hpp"
#include "LCM_IBEO_VEHICLE_STATE.hpp"
#include "LCM_MOBILEYE_INFO.hpp"


class LCM_SENSOR_FUSION_PACKAGE
{
    public:
        int64_t    PackInd;
        LCM_GPS_DATA Gps;
        LCM_IBEO_CLOUD_POINTS IbeoCloudPoints;
        LCM_IBEO_OBJECT_LIST IbeoObjList;
        int32_t    ContRadarObj;
        std::vector< LIDAR_RADAR_OBJECT_INFO > RadarList;
        LCM_NAVI_TO_SENSE_INFO NaviInfo;
        LCM_IBEO_VEHICLE_STATE VehicleState;
        LCM_MOBILEYE_INFO MobileyeInfo;

    public:
		inline LCM_SENSOR_FUSION_PACKAGE();
        inline int encode(void *buf, int offset, int maxlen) const;
        inline int getEncodedSize() const;
        inline int decode(const void *buf, int offset, int maxlen);
        inline static int64_t getHash();
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static int64_t _computeHash(const __lcm_hash_ptr *p);
};

LCM_SENSOR_FUSION_PACKAGE::LCM_SENSOR_FUSION_PACKAGE()
{
	ContRadarObj = 0;
}

int LCM_SENSOR_FUSION_PACKAGE::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int LCM_SENSOR_FUSION_PACKAGE::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int LCM_SENSOR_FUSION_PACKAGE::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t LCM_SENSOR_FUSION_PACKAGE::getHash()
{
    static int64_t hash = _computeHash(NULL);
    return hash;
}

const char* LCM_SENSOR_FUSION_PACKAGE::getTypeName()
{
    return "LCM_SENSOR_FUSION_PACKAGE";
}

int LCM_SENSOR_FUSION_PACKAGE::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &this->PackInd, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->Gps._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->IbeoCloudPoints._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->IbeoObjList._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->ContRadarObj, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->ContRadarObj; a0++) {
        tlen = this->RadarList[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = this->NaviInfo._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->VehicleState._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->MobileyeInfo._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int LCM_SENSOR_FUSION_PACKAGE::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this->PackInd, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->Gps._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->IbeoCloudPoints._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->IbeoObjList._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->ContRadarObj, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    this->RadarList.resize(this->ContRadarObj);
    for (int a0 = 0; a0 < this->ContRadarObj; a0++) {
        tlen = this->RadarList[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = this->NaviInfo._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->VehicleState._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->MobileyeInfo._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int LCM_SENSOR_FUSION_PACKAGE::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int64_t_encoded_array_size(NULL, 1);
    enc_size += this->Gps._getEncodedSizeNoHash();
    enc_size += this->IbeoCloudPoints._getEncodedSizeNoHash();
    enc_size += this->IbeoObjList._getEncodedSizeNoHash();
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->ContRadarObj; a0++) {
        enc_size += this->RadarList[a0]._getEncodedSizeNoHash();
    }
    enc_size += this->NaviInfo._getEncodedSizeNoHash();
    enc_size += this->VehicleState._getEncodedSizeNoHash();
    enc_size += this->MobileyeInfo._getEncodedSizeNoHash();
    return enc_size;
}

int64_t LCM_SENSOR_FUSION_PACKAGE::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == LCM_SENSOR_FUSION_PACKAGE::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, (void*)LCM_SENSOR_FUSION_PACKAGE::getHash };

    int64_t hash = 0xc64cf1e1a2a0fe01LL +
         LCM_GPS_DATA::_computeHash(&cp) +
         LCM_IBEO_CLOUD_POINTS::_computeHash(&cp) +
         LCM_IBEO_OBJECT_LIST::_computeHash(&cp) +
         LIDAR_RADAR_OBJECT_INFO::_computeHash(&cp) +
         LCM_NAVI_TO_SENSE_INFO::_computeHash(&cp) +
         LCM_IBEO_VEHICLE_STATE::_computeHash(&cp) +
         LCM_MOBILEYE_INFO::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

#endif
